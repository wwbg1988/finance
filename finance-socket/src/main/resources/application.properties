mode=${mode}

datasource.driverClassName=${datasource.driverClassName}
datasource.url=${datasource.url}
datasource.username=${datasource.username}
datasource.password=${datasource.password}

slave.datasource.driverClassName=${slave.datasource.driverClassName}
slave.datasource.url=${slave.datasource.url}
slave.datasource.username=${slave.datasource.username}
slave.datasource.password=${slave.datasource.password}

redisHost=${redisHost}
redisPort=${redisPort}
redisMaxTotal=${redisMaxTotal}
redisMaxIdle=${redisMaxIdle}
redisMaxWaitMillis=${redisMaxWaitMillis}
redisTestOnBorrow=${redisTestOnBorrow}

mongoHost=${mongoHost}
mongoPort=${mongoPort}

#datasource config
initialSize=${initialSize}
maxActive=${maxActive}
maxIdle=${maxIdle}
maxWait=${maxWait}

upload.url=${upload.url}

#sms phone
msg.url = ${msg.url}
msg.account = ${msg.account}
msg.password = ${msg.password}
msg.product = ${msg.product}
msg.extno = ${msg.extno}

#weixin
weixin.appId=${weixin.appId}
weixin.appSecret=${weixin.appSecret}
weixin.valid=${weixin.valid}

#kline
mp4Server.kline.address=${mp4Server.kline.address}
mp4Server.kline.port=${mp4Server.kline.port}

#server port
java.server.port=${java.server.port}

#next order
mp4Server.nextorder.address=${mp4Server.nextorder.address}
mp4Server.nextorder.port=${mp4Server.nextorder.port}
nextorder_profit_ratio=${nextorder_profit_ratio}

#history order
server.historyorder.port=${server.historyorder.port}

#activemq
activemq.topic.kline.name=${activemq.topic.kline.name}
activemq.queue.order.name=${activemq.queue.order.name}

#log
log4j.level=${log4j.level}